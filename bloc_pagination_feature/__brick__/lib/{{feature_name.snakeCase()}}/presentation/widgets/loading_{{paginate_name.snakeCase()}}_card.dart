import 'package:flutter/material.dart';

class Loading{{paginate_name.pascalCase()}}Card extends StatefulWidget {
  const Loading{{paginate_name.pascalCase()}}Card({
    super.key,
  });
  @override
  State<Loading{{paginate_name.pascalCase()}}Card> createState() => _Loading{{paginate_name.pascalCase()}}CardState();
}

class _Loading{{paginate_name.pascalCase()}}CardState extends State<Loading{{paginate_name.pascalCase()}}Card> {
  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
